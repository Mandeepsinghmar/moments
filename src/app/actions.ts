// "use server";

// import { db } from "~/server/db";

// export async function handleSubmitImage (e: React.FormEvent<HTMLFormElement>) {
//   const post = await db.query.posts.create({
//     data: {
//       name,
//     },
//   });
// };
